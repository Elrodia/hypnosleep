# Origin used for canonical and Open Graph/Twitter URLs in index.html.
# Overridden per-deployment (e.g. staging, preview) so crawlers and social
# previews resolve against the real deployment origin, not production.
VITE_SITE_ORIGIN=https://app.hypnosleep.app
