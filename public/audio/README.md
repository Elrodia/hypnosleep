# Landing page audio samples

Drop a short (≤15s) hypnosis preview at `sample.mp3`.

The landing page references `/audio/sample.mp3`. If the file is missing, the
"Listen to Sample" button surfaces a graceful "Sample unavailable" message
and the rest of the page works unchanged.
